info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: localhost:8080/login
  body:
    type: json
    data: |-
      {
        "id_token":"***REMOVED-TOKEN***"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
